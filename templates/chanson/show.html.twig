{% extends 'base.html.twig' %}

{% block title %}Chanson{% endblock %}

{% block body %}
    <h1>Chanson</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chanson.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ chanson.titre }}</td>
            </tr>
            <tr>
                <th>Artiste</th>
                <td>{{ chanson.artiste }}</td>
            </tr>
            <tr>
                <th>YoutubeId</th>
                <td>{{ chanson.youtubeId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chanson_index') }}">back to list</a>

    <a href="{{ path('chanson_edit', {'id': chanson.id}) }}">edit</a>

    {{ include('chanson/_delete_form.html.twig') }}
{% endblock %}
