{% extends 'base.html.twig' %}

{% block title %}Chanson index{% endblock %}

{% block body %}
    <h1>Chanson index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Artiste</th>
                <th>YoutubeId</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for chanson in chansons %}
            <tr>
                <td>{{ chanson.id }}</td>
                <td>{{ chanson.titre }}</td>
                <td>{{ chanson.artiste }}</td>
                <td>{{ chanson.youtubeId }}</td>
                <td>
                    <a href="{{ path('chanson_show', {'id': chanson.id}) }}">show</a>

                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('chanson_new') }}">Create new</a>
{% endblock %}
